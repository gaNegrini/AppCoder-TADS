export interface User {
    nome: String,
    email: String,
    password: String,
};

export interface newUser {
    nome: String,
    email: String,
    password: String,
    newemail: String
};

export interface userEmail {
    email: String
};
